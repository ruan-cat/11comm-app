# 整体迁移计划、杂项提示词文档

这个文档是从 `docs\prompts\migrate-plan\index.md` 继续接力而来的。

## 081 全面检查当前的项目的迁移改造进度

我现在不太清楚本项目的迁移改造进度如何了。请你认真阅读 `docs\prompts\route-migration-map.yml` 这份迁移映射表。看看本项目还有哪些模块没有完成旧项目到新项目的代码改造？

这是一个很艰巨的任务。请务必规划好多个子代理完成全面的检查和调研任务。

1. 新建多个并行的探索子代理，根据 `docs\prompts\route-migration-map.yml` 迁移映射表，检查清楚是否有完成迁移。
2. 如果出现了迁移文件，请你认真评估这些新的页面是否全面的，严格满足 skills 技能要求。
3. 最后给我出示一份非常详细的报告文档，全面的说明内容。

你很容易出现模块检索不到位的情况，请你列举出一个全面而详细探索路径表，完成检索。

每一个被检查的文件，你都应该要新建一个独立的，专项检查的子代理，逐项的对比检查是否严格按照每一个项目级别的 skills 文档要求来编写。不要出现任何缺漏。

这是一个非常艰巨的任务。我需要你先新建一个 openspec 任务，即 `check-migration-progress` 任务。以 openspec 任务为一个大任务的执行清单，然后逐步的生成一份巨大的检查报告。

将报告内容编写到 `docs\reports\2026-03-03-migration-progress-report.md` 内。

### 01 执行 `check-migration-progress` 任务

### 02 归档 `check-migration-progress` 任务

## 082 根据 `check-migration-progress` 任务，新建一个 openspec 任务批量完成代码迁移

请你认真阅读以下内容：

1. docs\reports\2026-03-03-migration-progress-report.md 报告。
2. openspec 的 `check-migration-progress` 任务。

我需要你根据 `docs\reports\2026-03-03-migration-progress-report.md` 这一份报告，新建一个非常详细的 openspec 任务。新建 openspec 任务时，请你务必使用 claude code 用户级别提供的 `openspec` 这个 skills 技能规范，按照这个 skills 技能规范来完成 openspec 的任务新建。

新建的 openspec 任务，需要完成`docs\reports\2026-03-03-migration-progress-report.md`报告所述的艰巨任务。要确保 openspec 的任务列表要极端的细致清晰，必须非常细致清晰。

确保在修改时，始终新建一个独立的子代理，完整的阅读全部的 skills 文档，严格依照 skills 文档的要求去修改每一个文件。

确保在修改每一个文件时，都能够新建单独的子代理，确保一个子代理修改一个文件。且及时的和审核子代理沟通审核。

在具体执行这款 openspec 任务时，不要贪快。确保处理一个路径对应的迁移任务时，新建 3 个并行的子代理完成迁移任务。新建 agent team 子代理团队完成任务。

### 新建任务列表

新建任务列表时，应该确保细粒度，且每一个映射对应的文件迁移路径，都能够有对应任务清单和修改项。

- 根据 `docs\prompts\route-migration-map.yml` 来新建非常多的任务。有多少个路径，就新建多少个任务项。
- 根据 `docs\reports\2026-03-03-migration-progress-report.md` 清单，明确清楚任务清单表需要做的项目，需要按照 skills 文档迁移的具体内容。

### 工作模式

你的工作模式如下：

1. 检查任务清单，按照顺序清单完成修改。
2. 一次只处理一个路径对应的文件。
3. 新建一个 agent team 子代理团队，新建 3 个子代理成员。
4. 完成任务后，删除掉一个具体的 `代码迁移子代理` 成员。
5. 标记一个路径对应的文件完成修改。
6. 阅读下一个任务清单项目，在现有的 agent team 团队内，在团队内新建一个新的 `代码迁移子代理` 成员，继续完成任务。

### 需要新建的子代理以及子代理之间的关系

1. `技能指导子代理`： 这个子代理负责全面的学习全部的 skills 文档，确保其清楚全部的代码编写规范。
2. `代码迁移子代理`： 这个子代理根据具体的任务清单，并且根据 `技能指导子代理` 的指导，完成迁移修改，或者是新建文件。
3. `审核子代理`： 负责根据 `技能指导子代理` 和 `代码迁移子代理` 的结果，完成

### 01 逐步的驱动模型执行 `complete-vue2-to-vue3-migration` 长任务

### 02 大批量的完成代码检查任务

本次修改修改了非常多的文件，为了避免后续提交代码时出现严重的语法错误而导致提交失败，我需要你完成对本次修改代码的语法检查。你的核心任务是确保代码不出现语法错误。

比如，错误的文件路径导入，错误的语法编写，缺漏括号，类型检查不合规等等情况。

本次修改和新增的代码很多，需要你新建多个并行的子代理成员，以 agent team 的形式完成任务。

你的工作模式如下：

1. 新建一个 agent team 子代理团队，新建 3 个子代理成员。
2. 完成任务后，删除掉一个具体的 `代码编写子代理` 成员。
3. 标记一个路径对应的文件完成修改。
4. 阅读下一个任务清单项目，在现有的 agent team 团队内，在团队内新建一个新的 `代码编写子代理` 成员，继续完成任务。

需要新建的子代理以及子代理之间的关系：

1. `技能指导子代理`： 这个子代理负责全面的学习全部的 skills 文档，确保其清楚全部的代码编写规范。
2. `代码编写子代理`： 这个子代理根据具体的任务清单，并且根据 `技能指导子代理` 的指导，完成迁移修改，或者是新建文件。
3. `审核子代理`： 负责根据 `技能指导子代理` 和 `代码编写子代理` 的结果，完成审核。

### 03 检查 `api-migration\references\api-定义示例.md` 规范是否过时？

已过时、已修改对应的 skills 技能。

---

ApiResponse 是正确的还是错误的规范？请你重点看看

请问 `.claude\skills\api-migration\references\api-定义示例.md` 的规范是不是过时了？和现在的实践截然相反？

还是说你认为需要改造，补充整个 api-migration 技能？

### 04 编写 git commit 提交信息

根据 `docs\reports\2026-03-04-api-response-unwrap-fix.md` 这一份报告文档，和本次对话信息，设计一个细粒度非常细致的 git commit 提交计划。并根据提交计划完成本次的 git commit 提交。

## 083 更新过时的 skills 文档

1. 更新过时的，误导的 skills。不要使用 `uni.showToast` 来完成提示，要用全局提示框组件来完成提示。根据 `.claude\skills\component-migration\references\全局反馈组件.md` 提供的组件，修改现存的 skills 文档。避免在继续使用过时的 `uni.showToast` 来完成提示。本项目使用的是 `component-migration\references\全局反馈组件.md` 所指导的文件来完成用户提示框。

2. 重新设计 skills 文档的触发提示文本，增强 skills 的触发能力。确保 skills 都能够被加载。在实际开发中，发现 .claude\skills\check-trigger.md 的作用不强，没有实现

## 084 <!-- 已完成，增加 z-index 来解决 --> 处理工作台滚动错位问题

如图：

![2026-03-24-18-56-01](https://gh-img-store.ruan-cat.com/img/2026-03-24-18-56-01.png)

![2026-03-24-18-56-21](https://gh-img-store.ruan-cat.com/img/2026-03-24-18-56-21.png)

滚动后，出现严重问题。底部的导航栏，竟然被遮盖遮挡了。这不对。我们滚动时，不应该出现这种情况。请修复。

页面路由： `http://localhost:3000/#/pages/work-dashboard/index`

请你使用谷歌浏览器 MCP 来处理。本地项目已经运行，不需要你重新启动一个本地服务。直接访问上面的页面路由即可。

## 085 <!-- 已初步完成改造，仍旧需要下一步的迭代 --> 优化首页的美观度

阅读以下报告和样式设计稿：

- docs\superpowers\specs\2026-03-24-homepage-business-portal-visual-design.md
- docs\superpowers\specs\2026-03-24-homepage-business-portal-visual-design-demo.html

优化，改造升级项目的首页。并对比首页的旧代码 `gitee-example/pages/index/index.vue` ，将必要的路由入口补全。

项目路由表： docs\prompts\route-migration-map.yml

---

较好的产出结果截图：

![2026-03-28-11-12-57](https://gh-img-store.ruan-cat.com/img/2026-03-28-11-12-57.png)

---

## 086 <!-- 已完成 个人觉得差不多了，没时间继续推进过渡完美了，可以了，直接去补全后端吧，确保落地 --> 对齐首页和工作台的样式风格

首页： http://localhost:3000/#/

![2026-03-28-11-58-02](https://gh-img-store.ruan-cat.com/img/2026-03-28-11-58-02.png)

工作台： http://localhost:3000/#/pages/work-dashboard/index

![2026-03-28-11-59-24](https://gh-img-store.ruan-cat.com/img/2026-03-28-11-59-24.png)

1. 目前首页恢复了颜色显示效果，但是整体看起来的效果太花了。不好看。请你适当的更改首页的 UI 设计，确保在保留 icon 颜色的情况下。美化首页；
2. 首页的标题，我希望你主动的使用工作台提供的标题，提供有动态渐变颜色能力的标题。
3. 我比较喜欢首页每一块卡片的背景渐变色。我需要你迁移，改造工作台每一个卡片的背景色，不要纯白色，提供合适的背景渐变色，确保美观大方。

## 087 <!-- 已完成 手动测试了一下，效果还行，大部分页面真的能进去，且效果不会太烂 --> 工作台页面补全，开启，激活合适的页面入口

现在的 `/pages/work-dashboard/index` 工作台，出现了太多不能使用的入口了，我希望这些入口都能够进入。

需要你酌情的分析，那些入口需要增加，并开启。

按照以下优先级来逐步增加工作台的可用入口。

1. 按照 `docs\vue2-route-navigation-map.md` 设计的路由入口来分析，根据这个设计，请你认真思考业务场景，做合理的推断和设计，在这个工作台内，增加对应业务页面的入口。便于用户在工作台内访问这些不同业务模块的功能。
2. 按照工作台旧代码 `gitee-example/pages/index/work.vue` 的提供可用入口。

## 088 <!-- 已完成 --> 将项目部署到 vercel 平台内

针对现在的 `build:h5:prod` 和 `build:nitro` 两个命令的构建产物，实现分别的 vercel 部署。我们这一个 app 项目，同时对外提供一个 H5 的生产环境，和一个 nitro v3 接口的生产环境。均部署到 vercel 平台内。

### 执行任务的注意事项、预先处理故障、和处理流程

1. 确保处理生产环境的时候，H5 的构建的生产环境模式下，其 baseUrl 为 `@ruan-cat/domains` 中 `11commAppNitroServer` 别名解析出的 Nitro 生产域名。
2. 确保 H5 生产环境的接口请求，做好提前处置接口跨域请求的故障。
3. 适当的在环境变量内，存储两个生产环境的生产环境域名。
4. 本项目的 github 仓库为： https://github.com/ruan-cat/11comm-app ，请注意导入这个 github 仓库，来完成 vercel 平台的部署。
5. 在 vercel 内完成对生产环境域名的配置，不需要额外检查是否通畅。我会手动的在 cloudflare 内配置域名的 CDN 的。
6. 上述流程如果出现卡壳，或者需要人工介入时，请及时向我寻求帮助。

### H5 的生产环境

- 构建命令： `build:h5:prod`
- vercel 项目名称： 11comm-app-h5
- 分配域名别名： `11commAppH5`

### nitro 的生产环境

- 构建命令： `build:nitro`
- vercel 项目名称： 11comm-app-nitro-server
- 分配域名别名： `11commAppNitroServer`

### 01 重新连接远程仓库 `ruan-cat/11comm-app`

本地 `origin` 应设为 `https://github.com/ruan-cat/11comm-app.git`；文档内 GitHub slug、Vercel 绑定说明与 VitePress `base` 已与 `11comm-app` 对齐。

我们的 vercel 还没有链接到远程仓库 `https://github.com/ruan-cat/11comm-app` ，请你继续尝试。

## 089 <!-- 莫名其妙的识别问题，写了main的兼容脚本即可解决 --> 处理项目项目页面路由，从工作台和首页进入后的页面，无法点击左上角的返回按钮而报错的故障

在生产环境内，即 `https://01s-11-app.ruan-cat.com/#/` 内，我们能明显复现出来，按钮点击后，进入到具体的功能页后，就无法点击左上角的返回按钮返回了。用谷歌浏览器 MCP，本地启动 `dev:h5:nitro` 命令，启动项目，并解决这个纯前端层面的错误。为什么无法实现基础的页面切换了？其路由跳转怎么了？

## 090 <!-- 已完成 --> 处理通讯录的页面问题

如图，通讯录竟然出现了双滚动条。滚动到最下面时，竟然会导致顶部的搜索栏被遮盖了。这是严重的样式写法问题。请修复通讯录页面的组件。

- 通讯录路由地址： /pages/address/list

![2026-03-30-20-17-35](https://gh-img-store.ruan-cat.com/img/2026-03-30-20-17-35.png)

### 01 还是不行，顶部搜索栏丢失了

![2026-03-30-20-51-18](https://gh-img-store.ruan-cat.com/img/2026-03-30-20-51-18.png)

## 091 <!-- TODO: --> 探索通用全面的微信小程序样式兼容写法技能和知识点

对于 vite + vue3 + typescript + uniapp，特别是我用 `unibest` 框架为基架，生成的微信小程序，其样式经常出现各种各样的不兼容问题，和样式错乱问题。

我在 uniapp 的 H5 模式下，构建效果挺好的。到了微信小程序就全部乱掉了。

请问针对微信小程序样式不兼容和丢失的情况，有没有通用一些的 skills 指导技能？或者是通用的指导文章？

我需要你帮我找找，业界常见的 uniapp -> 微信小程序 的样式兼容方案。 帮我找通用的兼容改写方案，我需要学习相关概论。避免踩坑；

---

可能的 url 参考链接：

- https://unibest.tech/base/4-style2
- https://unibest.tech/base/4-style

- https://uniapp.dcloud.net.cn/tutorial/platform.html
- https://uniapp.dcloud.net.cn/tutorial/syntax-css.html
- https://uniapp.dcloud.net.cn/api/ui/font.html

## 092 <!-- TODO: --> 处理普遍的微信小程序样式丢失问题

如下图所示，微信小程序内，pages/work-dashboard/index 的页面效果很差。样式丢失很严重。

![2026-03-29-12-20-15](https://gh-img-store.ruan-cat.com/img/2026-03-29-12-20-15.png)
