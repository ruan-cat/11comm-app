# 变量必须以 VITE_ 为前缀才能暴露给外部读取
NODE_ENV = 'production'

# 清空基础 env 中继承的地址，强制 Nitro 生产环境走 @ruan-cat/domains 的 11commAppNitroServer 别名解析
VITE_SERVER_BASEURL = ''
VITE_UPLOAD_BASEURL = ''
VITE_API_SECONDARY_URL = ''
VITE_API_RUNTIME = 'nitro-standalone'
VITE_APP_PROXY_ENABLE = false
NITRO_DATA_SOURCE = 'mock'
NITRO_PORT = 3101
